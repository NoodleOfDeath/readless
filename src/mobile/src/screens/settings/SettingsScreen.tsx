import React from 'react';

import { FlexView, SafeScrollView } from '~/components';

export function SettingsScreen() {
  return (
    <SafeScrollView>
      <FlexView row />
    </SafeScrollView>
  );
}
