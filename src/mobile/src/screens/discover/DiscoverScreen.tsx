import React from 'react';

import { FlexView, SafeScrollView } from '~/components';

export function DiscoverScreen() {
  return (
    <SafeScrollView>
      <FlexView row />
    </SafeScrollView>
  );
}
