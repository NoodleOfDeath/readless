import {
  PUBLIC_TOKEN_ATTRIBUTES,
  TokenAttributes,
  TokenCreationAttributes,
} from '../nlp/Token.types';

export type SentimentTokenAttributes = TokenAttributes;
export type SentimentTokenCreationAttributes = TokenCreationAttributes;

export const PUBLIC_SENTIMENT_TOKEN_ATTRIBUTES = PUBLIC_TOKEN_ATTRIBUTES;