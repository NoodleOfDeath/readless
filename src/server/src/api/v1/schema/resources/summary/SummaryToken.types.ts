import {
  PUBLIC_TOKEN_ATTRIBUTES,
  PublicTokenAttributes,
  TokenAttributes,
  TokenCreationAttributes,
} from '../nlp/Token.types';

export type SummaryTokenAttributes = TokenAttributes;
export type SummaryTokenCreationAttributes = TokenCreationAttributes;

export const PUBLIC_SUMMARY_TOKEN_ATTRIBUTES = PUBLIC_TOKEN_ATTRIBUTES;

export type PublicSummaryTokenAttributes = PublicTokenAttributes;