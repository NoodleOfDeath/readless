import {
  MediaAttributes,
  MediaCreationAttributes,
  PUBLIC_MEDIA_ATTRIBUTES,
  PublicMediaAttributes,
} from '../media/Media.types';

export type RecapMediaAttributes = MediaAttributes;
export type RecapMediaCreationAttributes = MediaCreationAttributes;

export const PUBLIC_RECAP_MEDIA_ATTRIBUTES = PUBLIC_MEDIA_ATTRIBUTES;

export type PublicRecapMediaAttributes = PublicMediaAttributes;