{
  "verbose": true,
  "ignore": ["**/*.sql"],
  "watch": [".env", "nodemon.json", "package.json", "src/**/*.ts", "tsconfig.json"],
  "ext": "css,html,js,json,ts",
  "exclude": ["node_modules"],
  "execMap": {
    "ts": "node --inspect=0.0.0.0:9229 --nolazy "
  }
}
